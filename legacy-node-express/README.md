# Legacy: Node/Express/BullMQ implementation

Arquivada. Este era o runtime original (Express + Prisma/PostgreSQL + BullMQ/Redis) do orquestrador
Shopify <-> Melhor Envio. Foi **substituido** pela versao 100% Supabase (Postgres + Edge Functions +
pg_cron) na raiz do repositorio. Mantido aqui apenas como referencia historica — nao rode isto em
producao, nao recebe mais atualizacoes.
